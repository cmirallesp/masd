include: Buyer.2apl;
include: Common.2apl;


beliefupdates:
  % needs: add a certain need
  {not searchProduct(Desc)} UpdateNeeds(Desc) {searchProduct(Desc)}

pcrules:

  event(updateNeeds(Desc), env) <- true | {
  UpdateNeeds(Desc);
  }
